import React from 'react';
import Header from '../components/Header';

const Wallet = () => (
  <div>
    <Header />
    <section>
      <h1>Wallet</h1>
    </section>
  </div>
);
export default Wallet;
